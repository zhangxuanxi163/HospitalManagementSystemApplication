# 修复登录问题

## 问题原因

数据库中的密码使用了BCrypt加密（`$2a$10$...`），但代码中使用MD5加密验证，两者不匹配导致登录失败。

## ✅ 已修复

已更新数据库中的admin用户密码为MD5加密格式。

## 🔐 默认登录账户

- **用户名**：`admin`
- **密码**：`admin123`
- **MD5加密后的密码**：`0192023a7bbd73250516f069df18b500`

## 🚀 现在可以登录了

1. 刷新登录页面
2. 输入用户名：`admin`
3. 输入密码：`admin123`
4. 点击登录

## 📝 如果还是无法登录

### 方法1：重新创建admin用户

在MySQL中执行：
```sql
USE hospital_db;
DELETE FROM users WHERE username='admin';
INSERT INTO users (username, password, real_name, role, department, status) 
VALUES ('admin', '0192023a7bbd73250516f069df18b500', '系统管理员', 'admin', '信息科', 1);
```

### 方法2：通过系统创建新用户

1. 如果可以通过其他方式登录，进入用户管理
2. 添加新用户，设置密码为 `admin123`
3. 系统会自动使用MD5加密

### 方法3：检查用户状态

确保用户状态为启用（status=1）：
```sql
USE hospital_db;
UPDATE users SET status=1 WHERE username='admin';
```

---

**现在请刷新页面重新登录试试！**







